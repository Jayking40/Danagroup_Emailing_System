// TODO: Implement useSearch hook
// - useSearchMail(query): useQuery(['search','mail',query]) → GET /api/search?q=&type=mail
// - useSearchUsers(query): useQuery(['search','users',query]) → GET /api/search?q=&type=users
// - Both enabled only when query.length >= 2
// - staleTime: 15_000

export function useSearch() {
  // TODO: Implement
}
