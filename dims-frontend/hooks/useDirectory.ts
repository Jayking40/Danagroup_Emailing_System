// TODO: Implement useDirectory hook
// - useEmployees(filters): useQuery(['users', filters]) → GET /api/users (paginated)
// - useEmployee(id): useQuery(['users', id]) → GET /api/users/:id
// - useSearchEmployees(query): useQuery(['users','search',query]) → GET /api/users/search?q=
//   enabled: query.length > 1, staleTime: 60_000

export function useDirectory() {
  // TODO: Implement
}
